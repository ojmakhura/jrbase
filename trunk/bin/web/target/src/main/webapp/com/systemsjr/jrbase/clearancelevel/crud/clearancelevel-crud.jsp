<!-- Generated by pages/crud/manageable.jsp.vsl in cartridge andromda-bpm4struts -->
<%@ include file="/taglib-imports.jspf" %>
<tiles:insert definition="main.layout">

    <tiles:put name="title" type="string">
        <bean:message key="clearance.level.page.title"/>
    </tiles:put>

    <tiles:put name="style" type="string">
        <link rel="stylesheet" type="text/css" media="screen" href="<html:rewrite page="/layout/default-manageable.css"/>"></link>
        <link rel="stylesheet" type="text/css" media="all" href="<html:rewrite page="/layout/default-calendar.css"/>"/>
    </tiles:put>

    <tiles:put name="javascript" type="string">
        <script type="text/javascript" src="<html:rewrite action="/calendar"/>"></script>
        <script type="text/javascript">
        //<!--
            function setSelect(multi, form, name, value)
            {
                var select = form.elements[name];
                var options = select.options;

                // for browser compatibility's sake we go through the options ourselves
                for (var i=0; i<options.length; i++)
                {
                    if (multi)
                    {
                        // Array.indexOf is defined in Javascript 1.5, not before
                        options[i].selected = arrayContainsElement(value,options[i].value);
                    }
                    else
                    {
                        if (options[i].value == value)
                        {
                            select.selectedIndex = i;
                            break;
                        }
                    }

                }
            }

            function arrayContainsElement(array, element)
            {
              var containsElement = false;
              for (var i=0; i<array.length && !containsElement; i++) containsElement = (array[i] == element);
              return containsElement;
            }

            function setAction(crud) { document.forms['manageClearanceLevelForm'].elements['crud'].value = crud; }

            function enableUpdate(enabled) { document.getElementById("updateButton").disabled = !enabled; }

            var selectionCount = 0;

            function verifyEnableDelete(checkbox)
            {
                if (checkbox.checked) selectionCount++; else selectionCount--;
                document.getElementById("deleteButton").disabled = (selectionCount < 1);
            }

            function clearFields(form)
            {
                form.reset();
                enableUpdate(false);
                setFields("","","","","","[]","[]",form);
            }

            function setFields(levelCode,levelDescription,id,lowerLevel,upperLevel,clearedRoles,clearedUsers,form)
            {
                form.elements["levelCode"].value = levelCode;
                form.elements["levelDescription"].value = levelDescription;
                form.elements["id"].value = id;
                setSelect(false,form,"lowerLevel",lowerLevel);
                setSelect(false,form,"upperLevel",upperLevel);
                setSelect(true,form,"clearedRoles",clearedRoles.substring(1,clearedRoles.length-1).split(", "));
                setSelect(true,form,"clearedUsers",clearedUsers.substring(1,clearedUsers.length-1).split(", "));
            }
        //-->
        </script>
    </tiles:put>

    <tiles:put name="body" type="string">

        <div>
            <h1><bean:message key="clearance.level.page.title"/></h1>
        </div>

        <html:form styleId="manageClearanceLevelForm" action="/ClearanceLevel/Manage" method="post">
            <input type="hidden" name="crud" value=""/>
            <div id="criteria">
                <c:if test="${!empty manageableForm}">
                    <table>
                        <tr>
                            <td><nobr><bean:message key="clearance.level.level.code"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:text name="manageableForm" property="levelCode" styleClass="criteriaField" styleId="levelCode"/>
                            </td>
                        </tr>
                        <tr>
                            <td><nobr><bean:message key="clearance.level.level.description"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:textarea name="manageableForm" property="levelDescription" styleClass="criteriaField" styleId="levelDescription"/>
                            </td>
                        </tr>
                    <html:hidden name="manageableForm" property="id"/>
                        <tr>
                            <td>
                                    <html:link action="/ClearanceLevel/Manage"><bean:message key="clearance.level.lower.level"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.lowerLevelBackingList}">
                                        <select name="lowerLevel" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="lowerLevel">
                                            <option value=""><bean:message key="select.option.blank"/></option>
                                            <c:forEach var="valueLabel" items="${manageableForm.lowerLevelBackingList}">
                                                <c:choose>
                                                    <c:when test="${valueLabel[0] eq manageableForm.lowerLevel}">
                                                        <option value="${valueLabel[0]}" selected="selected">${valueLabel[1]}</option>
                                                    </c:when>
                                                    <c:otherwise>
                                                        <option value="${valueLabel[0]}">${valueLabel[1]}</option>
                                                    </c:otherwise>
                                                </c:choose>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/ClearanceLevel/Manage"><bean:message key="clearance.level.upper.level"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.upperLevelBackingList}">
                                        <select name="upperLevel" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="upperLevel">
                                            <option value=""><bean:message key="select.option.blank"/></option>
                                            <c:forEach var="valueLabel" items="${manageableForm.upperLevelBackingList}">
                                                <c:choose>
                                                    <c:when test="${valueLabel[0] eq manageableForm.upperLevel}">
                                                        <option value="${valueLabel[0]}" selected="selected">${valueLabel[1]}</option>
                                                    </c:when>
                                                    <c:otherwise>
                                                        <option value="${valueLabel[0]}">${valueLabel[1]}</option>
                                                    </c:otherwise>
                                                </c:choose>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/Role/Manage"><bean:message key="clearance.level.cleared.roles"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.clearedRolesBackingList}">
                                        <select name="clearedRoles" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="clearedRoles" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.clearedRolesBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.clearedRoles}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/User/Manage"><bean:message key="clearance.level.cleared.users"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.clearedUsersBackingList}">
                                        <select name="clearedUsers" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="clearedUsers" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.clearedUsersBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.clearedUsers}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                    </table>

                    <input type="submit" id="readButton" class="button" value="<bean:message key="button.read"/>" onclick="this.form.elements['id'].value='';setAction('read');"/>
                    <input type="submit" id="createButton" class="button" value="<bean:message key="button.create"/>" onclick="setAction('create');"/>
                    <input type="submit" id="deleteButton" class="button" value="<bean:message key="button.delete"/>" disabled="disabled" onclick="setAction('delete');"/>
                    <input type="submit" id="updateButton" class="button" value="<bean:message key="button.update"/>" disabled="disabled" onclick="setAction('update');return validateUpdate();"/>
                    <input type="button" id="clearButton" class="button" value="<bean:message key="button.clear"/>" onclick="clearFields(this.form);"/>
                </c:if>

<div id="entitySwitcher">
    <nobr>
        <bean:message key="select.other.entity"/>
        <select onchange="document.location=this.options[this.selectedIndex].value+'?ref_ClearanceLevel='+this.form.elements['id'].value;">
            <option selected="selected" value="<html:rewrite page="/ClearanceLevel/Manage.do"/>"><bean:message key="clearance.level"/></option>
            <option value="<html:rewrite page="/Counter/Manage.do"/>"><bean:message key="counter"/></option>
            <option value="<html:rewrite page="/Individual/Manage.do"/>"><bean:message key="individual"/></option>
            <option value="<html:rewrite page="/Location/Manage.do"/>"><bean:message key="location"/></option>
            <option value="<html:rewrite page="/LocationType/Manage.do"/>"><bean:message key="location.type"/></option>
            <option value="<html:rewrite page="/LoginSession/Manage.do"/>"><bean:message key="login.session"/></option>
            <option value="<html:rewrite page="/Menu/Manage.do"/>"><bean:message key="menu"/></option>
            <option value="<html:rewrite page="/Organisation/Manage.do"/>"><bean:message key="organisation"/></option>
            <option value="<html:rewrite page="/OrganisationType/Manage.do"/>"><bean:message key="organisation.type"/></option>
            <option value="<html:rewrite page="/Program/Manage.do"/>"><bean:message key="program"/></option>
            <option value="<html:rewrite page="/Role/Manage.do"/>"><bean:message key="role"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/User/Manage.do"/>"><bean:message key="user"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/UserLocation/Manage.do"/>"><bean:message key="user.location"/></option>
        </select>
    </nobr>
</div>

            </div>

            <div id="manageableList" class="table">
                <c:if test="${!empty manageableForm.manageableList}">
                    <display:table name="${manageableForm.manageableList}" id="row" requestURI="${pageContext.request.requestURI}"
                            requestURIcontext="false"
                            export="true" pagesize="15" sort="list">
                        <display:column media="html" sortable="false">
                            <nobr>
                                <input type="radio" name="_copy" onclick="enableUpdate(true);setFields('<formatting:escape language="javascript">${row.levelCode}</formatting:escape>','<formatting:escape language="javascript">${row.levelDescription}</formatting:escape>','<formatting:escape language="javascript">${row.id}</formatting:escape>','<formatting:escape language="javascript">${row.lowerLevel}</formatting:escape>','<formatting:escape language="javascript">${row.upperLevel}</formatting:escape>','<formatting:escape language="javascript">${row.clearedRoles}</formatting:escape>','<formatting:escape language="javascript">${row.clearedUsers}</formatting:escape>',this.form);"/>
                                <input type="checkbox" name="selectedRows" value="${row.id}" onclick="verifyEnableDelete(this);"/>
                            </nobr>
                        </display:column>
                        <display:column media="xml csv excel pdf"
                            property="levelCode"
                            titleKey="clearance.level.level.code"/>
                        <display:column media="html"
                            headerClass="levelCode" paramId="levelCode" maxLength="47"
                            sortProperty="levelCode" sortable="true"
                            titleKey="clearance.level.level.code"><nobr><formatting:escape language="javascript,html">${row.levelCode}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="levelDescription"
                            titleKey="clearance.level.level.description"/>
                        <display:column media="html"
                            headerClass="levelDescription" paramId="levelDescription" maxLength="47"
                            sortProperty="levelDescription" sortable="true"
                            titleKey="clearance.level.level.description"><nobr><formatting:escape language="javascript,html">${row.levelDescription}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="lowerLevelLabel"
                            titleKey="clearance.level.lower.level"/>
                        <display:column media="html"
                            headerClass="lowerLevel" paramId="lowerLevel" maxLength="47"
                            sortProperty="lowerLevelLabel" sortable="true"
                            titleKey="clearance.level.lower.level"><nobr><formatting:escape language="javascript,html">${row.lowerLevelLabel}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="upperLevelLabel"
                            titleKey="clearance.level.upper.level"/>
                        <display:column media="html"
                            headerClass="upperLevel" paramId="upperLevel" maxLength="47"
                            sortProperty="upperLevelLabel" sortable="true"
                            titleKey="clearance.level.upper.level"><nobr><formatting:escape language="javascript,html">${row.upperLevelLabel}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="clearedRolesLabels"
                            titleKey="clearance.level.cleared.roles"/>
                        <display:column media="html"
                            headerClass="clearedRoles" paramId="clearedRoles" maxLength="47"
                            sortProperty="clearedRolesLabels" sortable="true"
                            titleKey="clearance.level.cleared.roles"><nobr><formatting:escape language="javascript,html">${row.clearedRolesLabels}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="clearedUsersLabels"
                            titleKey="clearance.level.cleared.users"/>
                        <display:column media="html"
                            headerClass="clearedUsers" paramId="clearedUsers" maxLength="47"
                            sortProperty="clearedUsersLabels" sortable="true"
                            titleKey="clearance.level.cleared.users"><nobr><formatting:escape language="javascript,html">${row.clearedUsersLabels}</formatting:escape></nobr></display:column>
                    </display:table>
                </c:if>
            </div>

        </html:form>

        <div id="pageHelpSection">
            <blockquote>
                <a href="" id="pageHelp" style="display:inline;" onclick="openWindow('<html:rewrite action="/ClearanceLevel/ManageHelp"/>','onlinehelp',true,false,760,540); return false;">
                    <bean:message key="online.help.href"/>
                </a>
                <html:img page="/layout/help.gif" style="display:inline;"/>
            </blockquote>
        </div>

    </tiles:put>

</tiles:insert>

