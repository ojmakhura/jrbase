<!-- Generated by pages/crud/manageable.jsp.vsl in cartridge andromda-bpm4struts -->
<%@ include file="/taglib-imports.jspf" %>
<tiles:insert definition="main.layout">

    <tiles:put name="title" type="string">
        <bean:message key="role.page.title"/>
    </tiles:put>

    <tiles:put name="style" type="string">
        <link rel="stylesheet" type="text/css" media="screen" href="<html:rewrite page="/layout/default-manageable.css"/>"></link>
        <link rel="stylesheet" type="text/css" media="all" href="<html:rewrite page="/layout/default-calendar.css"/>"/>
    </tiles:put>

    <tiles:put name="javascript" type="string">
        <script type="text/javascript" src="<html:rewrite action="/calendar"/>"></script>
        <script type="text/javascript">
        //<!--
            function setSelect(multi, form, name, value)
            {
                var select = form.elements[name];
                var options = select.options;

                // for browser compatibility's sake we go through the options ourselves
                for (var i=0; i<options.length; i++)
                {
                    if (multi)
                    {
                        // Array.indexOf is defined in Javascript 1.5, not before
                        options[i].selected = arrayContainsElement(value,options[i].value);
                    }
                    else
                    {
                        if (options[i].value == value)
                        {
                            select.selectedIndex = i;
                            break;
                        }
                    }

                }
            }

            function arrayContainsElement(array, element)
            {
              var containsElement = false;
              for (var i=0; i<array.length && !containsElement; i++) containsElement = (array[i] == element);
              return containsElement;
            }

            function setAction(crud) { document.forms['manageRoleForm'].elements['crud'].value = crud; }

            function enableUpdate(enabled) { document.getElementById("updateButton").disabled = !enabled; }

            var selectionCount = 0;

            function verifyEnableDelete(checkbox)
            {
                if (checkbox.checked) selectionCount++; else selectionCount--;
                document.getElementById("deleteButton").disabled = (selectionCount < 1);
            }

            function clearFields(form)
            {
                form.reset();
                enableUpdate(false);
                setFields("","","","","[]","[]","[]","[]",form);
            }

            function setFields(roleCode,role,roleDescription,id,programs,roleClearanceLevels,users,menus,form)
            {
                form.elements["roleCode"].value = roleCode;
                form.elements["role"].value = role;
                form.elements["roleDescription"].value = roleDescription;
                form.elements["id"].value = id;
                setSelect(true,form,"programs",programs.substring(1,programs.length-1).split(", "));
                setSelect(true,form,"roleClearanceLevels",roleClearanceLevels.substring(1,roleClearanceLevels.length-1).split(", "));
                setSelect(true,form,"users",users.substring(1,users.length-1).split(", "));
                setSelect(true,form,"menus",menus.substring(1,menus.length-1).split(", "));
            }
        //-->
        </script>
    </tiles:put>

    <tiles:put name="body" type="string">

        <div>
            <h1><bean:message key="role.page.title"/></h1>
        </div>

        <html:form styleId="manageRoleForm" action="/Role/Manage" method="post">
            <input type="hidden" name="crud" value=""/>
            <div id="criteria">
                <c:if test="${!empty manageableForm}">
                    <table>
                        <tr>
                            <td><nobr><bean:message key="role.role.code"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:text name="manageableForm" property="roleCode" styleClass="criteriaField" styleId="roleCode"/>
                            </td>
                        </tr>
                        <tr>
                            <td><nobr><bean:message key="role.role"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:text name="manageableForm" property="role" styleClass="criteriaField" styleId="role"/>
                            </td>
                        </tr>
                        <tr>
                            <td><nobr><bean:message key="role.role.description"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:textarea name="manageableForm" property="roleDescription" styleClass="criteriaField" styleId="roleDescription"/>
                            </td>
                        </tr>
                    <html:hidden name="manageableForm" property="id"/>
                        <tr>
                            <td>
                                    <html:link action="/Program/Manage"><bean:message key="role.programs"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.programsBackingList}">
                                        <select name="programs" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="programs" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.programsBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.programs}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/ClearanceLevel/Manage"><bean:message key="role.role.clearance.levels"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.roleClearanceLevelsBackingList}">
                                        <select name="roleClearanceLevels" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="roleClearanceLevels" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.roleClearanceLevelsBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.roleClearanceLevels}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/User/Manage"><bean:message key="role.users"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.usersBackingList}">
                                        <select name="users" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="users" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.usersBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.users}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/Menu/Manage"><bean:message key="role.menus"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.menusBackingList}">
                                        <select name="menus" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="menus" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.menusBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.menus}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                    </table>

                    <input type="submit" id="readButton" class="button" value="<bean:message key="button.read"/>" onclick="this.form.elements['id'].value='';setAction('read');"/>
                    <input type="submit" id="createButton" class="button" value="<bean:message key="button.create"/>" onclick="setAction('create');"/>
                    <input type="submit" id="deleteButton" class="button" value="<bean:message key="button.delete"/>" disabled="disabled" onclick="setAction('delete');"/>
                    <input type="submit" id="updateButton" class="button" value="<bean:message key="button.update"/>" disabled="disabled" onclick="setAction('update');return validateUpdate();"/>
                    <input type="button" id="clearButton" class="button" value="<bean:message key="button.clear"/>" onclick="clearFields(this.form);"/>
                </c:if>

<div id="entitySwitcher">
    <nobr>
        <bean:message key="select.other.entity"/>
        <select onchange="document.location=this.options[this.selectedIndex].value+'?ref_Role='+this.form.elements['id'].value;">
            <option value="<html:rewrite page="/ClearanceLevel/Manage.do"/>"><bean:message key="clearance.level"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/Counter/Manage.do"/>"><bean:message key="counter"/></option>
            <option value="<html:rewrite page="/Individual/Manage.do"/>"><bean:message key="individual"/></option>
            <option value="<html:rewrite page="/Location/Manage.do"/>"><bean:message key="location"/></option>
            <option value="<html:rewrite page="/LocationType/Manage.do"/>"><bean:message key="location.type"/></option>
            <option value="<html:rewrite page="/LoginSession/Manage.do"/>"><bean:message key="login.session"/></option>
            <option value="<html:rewrite page="/Menu/Manage.do"/>"><bean:message key="menu"/></option>
            <option value="<html:rewrite page="/Organisation/Manage.do"/>"><bean:message key="organisation"/></option>
            <option value="<html:rewrite page="/OrganisationType/Manage.do"/>"><bean:message key="organisation.type"/></option>
            <option value="<html:rewrite page="/Program/Manage.do"/>"><bean:message key="program"/></option>
            <option selected="selected" value="<html:rewrite page="/Role/Manage.do"/>"><bean:message key="role"/></option>
            <option value="<html:rewrite page="/User/Manage.do"/>"><bean:message key="user"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/UserLocation/Manage.do"/>"><bean:message key="user.location"/></option>
        </select>
    </nobr>
</div>

            </div>

            <div id="manageableList" class="table">
                <c:if test="${!empty manageableForm.manageableList}">
                    <display:table name="${manageableForm.manageableList}" id="row" requestURI="${pageContext.request.requestURI}"
                            requestURIcontext="false"
                            export="true" pagesize="15" sort="list">
                        <display:column media="html" sortable="false">
                            <nobr>
                                <input type="radio" name="_copy" onclick="enableUpdate(true);setFields('<formatting:escape language="javascript">${row.roleCode}</formatting:escape>','<formatting:escape language="javascript">${row.role}</formatting:escape>','<formatting:escape language="javascript">${row.roleDescription}</formatting:escape>','<formatting:escape language="javascript">${row.id}</formatting:escape>','<formatting:escape language="javascript">${row.programs}</formatting:escape>','<formatting:escape language="javascript">${row.roleClearanceLevels}</formatting:escape>','<formatting:escape language="javascript">${row.users}</formatting:escape>','<formatting:escape language="javascript">${row.menus}</formatting:escape>',this.form);"/>
                                <input type="checkbox" name="selectedRows" value="${row.id}" onclick="verifyEnableDelete(this);"/>
                            </nobr>
                        </display:column>
                        <display:column media="xml csv excel pdf"
                            property="roleCode"
                            titleKey="role.role.code"/>
                        <display:column media="html"
                            headerClass="roleCode" paramId="roleCode" maxLength="47"
                            sortProperty="roleCode" sortable="true"
                            titleKey="role.role.code"><nobr><formatting:escape language="javascript,html">${row.roleCode}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="role"
                            titleKey="role.role"/>
                        <display:column media="html"
                            headerClass="role" paramId="role" maxLength="47"
                            sortProperty="role" sortable="true"
                            titleKey="role.role"><nobr><formatting:escape language="javascript,html">${row.role}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="roleDescription"
                            titleKey="role.role.description"/>
                        <display:column media="html"
                            headerClass="roleDescription" paramId="roleDescription" maxLength="47"
                            sortProperty="roleDescription" sortable="true"
                            titleKey="role.role.description"><nobr><formatting:escape language="javascript,html">${row.roleDescription}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="programsLabels"
                            titleKey="role.programs"/>
                        <display:column media="html"
                            headerClass="programs" paramId="programs" maxLength="47"
                            sortProperty="programsLabels" sortable="true"
                            titleKey="role.programs"><nobr><formatting:escape language="javascript,html">${row.programsLabels}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="roleClearanceLevelsLabels"
                            titleKey="role.role.clearance.levels"/>
                        <display:column media="html"
                            headerClass="roleClearanceLevels" paramId="roleClearanceLevels" maxLength="47"
                            sortProperty="roleClearanceLevelsLabels" sortable="true"
                            titleKey="role.role.clearance.levels"><nobr><formatting:escape language="javascript,html">${row.roleClearanceLevelsLabels}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="usersLabels"
                            titleKey="role.users"/>
                        <display:column media="html"
                            headerClass="users" paramId="users" maxLength="47"
                            sortProperty="usersLabels" sortable="true"
                            titleKey="role.users"><nobr><formatting:escape language="javascript,html">${row.usersLabels}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="menusLabels"
                            titleKey="role.menus"/>
                        <display:column media="html"
                            headerClass="menus" paramId="menus" maxLength="47"
                            sortProperty="menusLabels" sortable="true"
                            titleKey="role.menus"><nobr><formatting:escape language="javascript,html">${row.menusLabels}</formatting:escape></nobr></display:column>
                    </display:table>
                </c:if>
            </div>

        </html:form>

        <div id="pageHelpSection">
            <blockquote>
                <a href="" id="pageHelp" style="display:inline;" onclick="openWindow('<html:rewrite action="/Role/ManageHelp"/>','onlinehelp',true,false,760,540); return false;">
                    <bean:message key="online.help.href"/>
                </a>
                <html:img page="/layout/help.gif" style="display:inline;"/>
            </blockquote>
        </div>

    </tiles:put>

</tiles:insert>

