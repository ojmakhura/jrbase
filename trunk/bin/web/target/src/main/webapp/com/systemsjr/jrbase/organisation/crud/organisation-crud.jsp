<!-- Generated by pages/crud/manageable.jsp.vsl in cartridge andromda-bpm4struts -->
<%@ include file="/taglib-imports.jspf" %>
<tiles:insert definition="main.layout">

    <tiles:put name="title" type="string">
        <bean:message key="organisation.page.title"/>
    </tiles:put>

    <tiles:put name="style" type="string">
        <link rel="stylesheet" type="text/css" media="screen" href="<html:rewrite page="/layout/default-manageable.css"/>"></link>
        <link rel="stylesheet" type="text/css" media="all" href="<html:rewrite page="/layout/default-calendar.css"/>"/>
    </tiles:put>

    <tiles:put name="javascript" type="string">
        <script type="text/javascript" src="<html:rewrite action="/calendar"/>"></script>
        <script type="text/javascript">
        //<!--
            function setSelect(multi, form, name, value)
            {
                var select = form.elements[name];
                var options = select.options;

                // for browser compatibility's sake we go through the options ourselves
                for (var i=0; i<options.length; i++)
                {
                    if (multi)
                    {
                        // Array.indexOf is defined in Javascript 1.5, not before
                        options[i].selected = arrayContainsElement(value,options[i].value);
                    }
                    else
                    {
                        if (options[i].value == value)
                        {
                            select.selectedIndex = i;
                            break;
                        }
                    }

                }
            }

            function arrayContainsElement(array, element)
            {
              var containsElement = false;
              for (var i=0; i<array.length && !containsElement; i++) containsElement = (array[i] == element);
              return containsElement;
            }

            function setAction(crud) { document.forms['manageOrganisationForm'].elements['crud'].value = crud; }

            function enableUpdate(enabled) { document.getElementById("updateButton").disabled = !enabled; }

            var selectionCount = 0;

            function verifyEnableDelete(checkbox)
            {
                if (checkbox.checked) selectionCount++; else selectionCount--;
                document.getElementById("deleteButton").disabled = (selectionCount < 1);
            }

            function clearFields(form)
            {
                form.reset();
                enableUpdate(false);
                setFields("","","","","","[]","","[]",form);
            }

            function setFields(registrationRef,name,description,id,organisationType,organisationLocations,organisation,organisations,form)
            {
                form.elements["registrationRef"].value = registrationRef;
                form.elements["name"].value = name;
                form.elements["description"].value = description;
                form.elements["id"].value = id;
                setSelect(false,form,"organisationType",organisationType);
                setSelect(true,form,"organisationLocations",organisationLocations.substring(1,organisationLocations.length-1).split(", "));
                setSelect(false,form,"organisation",organisation);
                setSelect(true,form,"organisations",organisations.substring(1,organisations.length-1).split(", "));
            }
        //-->
        </script>
    </tiles:put>

    <tiles:put name="body" type="string">

        <div>
            <h1><bean:message key="organisation.page.title"/></h1>
        </div>

        <html:form styleId="manageOrganisationForm" action="/Organisation/Manage" method="post">
            <input type="hidden" name="crud" value=""/>
            <div id="criteria">
                <c:if test="${!empty manageableForm}">
                    <table>
                        <tr>
                            <td><nobr><bean:message key="organisation.registration.ref"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:text name="manageableForm" property="registrationRef" styleClass="criteriaField" styleId="registrationRef"/>
                            </td>
                        </tr>
                        <tr>
                            <td><nobr><bean:message key="organisation.name"/> <div class="important">*</div></nobr></td>
                            <td>
                                <html:text name="manageableForm" property="name" styleClass="criteriaField" styleId="name"/>
                            </td>
                        </tr>
                        <tr>
                            <td><bean:message key="organisation.description"/></td>
                            <td>
                                <html:textarea name="manageableForm" property="description" styleClass="criteriaField" styleId="description"/>
                            </td>
                        </tr>
                    <html:hidden name="manageableForm" property="id"/>
                        <tr>
                            <td>
                                    <nobr><html:link action="/OrganisationType/Manage"><bean:message key="organisation.organisation.type"/></html:link> <div class="important">*</div></nobr>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.organisationTypeBackingList}">
                                        <select name="organisationType" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="organisationType">
                                            <option value=""><bean:message key="select.option.blank"/></option>
                                            <c:forEach var="valueLabel" items="${manageableForm.organisationTypeBackingList}">
                                                <c:choose>
                                                    <c:when test="${valueLabel[0] eq manageableForm.organisationType}">
                                                        <option value="${valueLabel[0]}" selected="selected">${valueLabel[1]}</option>
                                                    </c:when>
                                                    <c:otherwise>
                                                        <option value="${valueLabel[0]}">${valueLabel[1]}</option>
                                                    </c:otherwise>
                                                </c:choose>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/Location/Manage"><bean:message key="organisation.organisation.locations"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.organisationLocationsBackingList}">
                                        <select name="organisationLocations" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="organisationLocations" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.organisationLocationsBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.organisationLocations}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/Organisation/Manage"><bean:message key="organisation.organisation"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.organisationBackingList}">
                                        <select name="organisation" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="organisation">
                                            <option value=""><bean:message key="select.option.blank"/></option>
                                            <c:forEach var="valueLabel" items="${manageableForm.organisationBackingList}">
                                                <c:choose>
                                                    <c:when test="${valueLabel[0] eq manageableForm.organisation}">
                                                        <option value="${valueLabel[0]}" selected="selected">${valueLabel[1]}</option>
                                                    </c:when>
                                                    <c:otherwise>
                                                        <option value="${valueLabel[0]}">${valueLabel[1]}</option>
                                                    </c:otherwise>
                                                </c:choose>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                    <html:link action="/Organisation/Manage"><bean:message key="organisation.organisations"/></html:link>
                            </td>
                            <td>
                                <c:choose>
                                    <c:when test="${empty manageableForm.organisationsBackingList}">
                                        <select name="organisations" disabled="disabled"/>
                                    </c:when>
                                    <c:otherwise>
                                        <select name="organisations" multiple="multiple">
                                            <c:forEach var="valueLabel" items="${manageableForm.organisationsBackingList}">
                        <option value="${valueLabel[0]}"<collections:contains item="${valueLabel}" array="${manageableForm.organisations}"> selected="selected"</collections:contains>>${valueLabel[1]}</option>
                                            </c:forEach>
                                        </select>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                        </tr>
                    </table>

                    <input type="submit" id="readButton" class="button" value="<bean:message key="button.read"/>" onclick="this.form.elements['id'].value='';setAction('read');"/>
                    <input type="submit" id="createButton" class="button" value="<bean:message key="button.create"/>" onclick="setAction('create');"/>
                    <input type="submit" id="deleteButton" class="button" value="<bean:message key="button.delete"/>" disabled="disabled" onclick="setAction('delete');"/>
                    <input type="submit" id="updateButton" class="button" value="<bean:message key="button.update"/>" disabled="disabled" onclick="setAction('update');return validateUpdate();"/>
                    <input type="button" id="clearButton" class="button" value="<bean:message key="button.clear"/>" onclick="clearFields(this.form);"/>
                </c:if>

<div id="entitySwitcher">
    <nobr>
        <bean:message key="select.other.entity"/>
        <select onchange="document.location=this.options[this.selectedIndex].value+'?ref_Organisation='+this.form.elements['id'].value;">
            <option value="<html:rewrite page="/ClearanceLevel/Manage.do"/>"><bean:message key="clearance.level"/></option>
            <option value="<html:rewrite page="/Counter/Manage.do"/>"><bean:message key="counter"/></option>
            <option value="<html:rewrite page="/Individual/Manage.do"/>"><bean:message key="individual"/></option>
            <option value="<html:rewrite page="/Location/Manage.do"/>"><bean:message key="location"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/LocationType/Manage.do"/>"><bean:message key="location.type"/></option>
            <option value="<html:rewrite page="/LoginSession/Manage.do"/>"><bean:message key="login.session"/></option>
            <option value="<html:rewrite page="/Menu/Manage.do"/>"><bean:message key="menu"/></option>
            <option selected="selected" value="<html:rewrite page="/Organisation/Manage.do"/>"><bean:message key="organisation"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/OrganisationType/Manage.do"/>"><bean:message key="organisation.type"/><bean:message key="referencing.entity.marker"/></option>
            <option value="<html:rewrite page="/Program/Manage.do"/>"><bean:message key="program"/></option>
            <option value="<html:rewrite page="/Role/Manage.do"/>"><bean:message key="role"/></option>
            <option value="<html:rewrite page="/User/Manage.do"/>"><bean:message key="user"/></option>
            <option value="<html:rewrite page="/UserLocation/Manage.do"/>"><bean:message key="user.location"/></option>
        </select>
    </nobr>
</div>

            </div>

            <div id="manageableList" class="table">
                <c:if test="${!empty manageableForm.manageableList}">
                    <display:table name="${manageableForm.manageableList}" id="row" requestURI="${pageContext.request.requestURI}"
                            requestURIcontext="false"
                            export="true" pagesize="15" sort="list">
                        <display:column media="html" sortable="false">
                            <nobr>
                                <input type="radio" name="_copy" onclick="enableUpdate(true);setFields('<formatting:escape language="javascript">${row.registrationRef}</formatting:escape>','<formatting:escape language="javascript">${row.name}</formatting:escape>','<formatting:escape language="javascript">${row.description}</formatting:escape>','<formatting:escape language="javascript">${row.id}</formatting:escape>','<formatting:escape language="javascript">${row.organisationType}</formatting:escape>','<formatting:escape language="javascript">${row.organisationLocations}</formatting:escape>','<formatting:escape language="javascript">${row.organisation}</formatting:escape>','<formatting:escape language="javascript">${row.organisations}</formatting:escape>',this.form);"/>
                                <input type="checkbox" name="selectedRows" value="${row.id}" onclick="verifyEnableDelete(this);"/>
                            </nobr>
                        </display:column>
                        <display:column media="xml csv excel pdf"
                            property="registrationRef"
                            titleKey="organisation.registration.ref"/>
                        <display:column media="html"
                            headerClass="registrationRef" paramId="registrationRef" maxLength="47"
                            sortProperty="registrationRef" sortable="true"
                            titleKey="organisation.registration.ref"><nobr><formatting:escape language="javascript,html">${row.registrationRef}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="name"
                            titleKey="organisation.name"/>
                        <display:column media="html"
                            headerClass="name" paramId="name" maxLength="47"
                            sortProperty="name" sortable="true"
                            titleKey="organisation.name"><nobr><formatting:escape language="javascript,html">${row.name}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="description"
                            titleKey="organisation.description"/>
                        <display:column media="html"
                            headerClass="description" paramId="description" maxLength="47"
                            sortProperty="description" sortable="true"
                            titleKey="organisation.description"><nobr><formatting:escape language="javascript,html">${row.description}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="organisationTypeLabel"
                            titleKey="organisation.organisation.type"/>
                        <display:column media="html"
                            headerClass="organisationType" paramId="organisationType" maxLength="47"
                            sortProperty="organisationTypeLabel" sortable="true"
                            titleKey="organisation.organisation.type"><nobr><formatting:escape language="javascript,html">${row.organisationTypeLabel}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="organisationLocationsLabels"
                            titleKey="organisation.organisation.locations"/>
                        <display:column media="html"
                            headerClass="organisationLocations" paramId="organisationLocations" maxLength="47"
                            sortProperty="organisationLocationsLabels" sortable="true"
                            titleKey="organisation.organisation.locations"><nobr><formatting:escape language="javascript,html">${row.organisationLocationsLabels}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="organisationLabel"
                            titleKey="organisation.organisation"/>
                        <display:column media="html"
                            headerClass="organisation" paramId="organisation" maxLength="47"
                            sortProperty="organisationLabel" sortable="true"
                            titleKey="organisation.organisation"><nobr><formatting:escape language="javascript,html">${row.organisationLabel}</formatting:escape></nobr></display:column>
                        <display:column media="xml csv excel pdf"
                            property="organisationsLabels"
                            titleKey="organisation.organisations"/>
                        <display:column media="html"
                            headerClass="organisations" paramId="organisations" maxLength="47"
                            sortProperty="organisationsLabels" sortable="true"
                            titleKey="organisation.organisations"><nobr><formatting:escape language="javascript,html">${row.organisationsLabels}</formatting:escape></nobr></display:column>
                    </display:table>
                </c:if>
            </div>

        </html:form>

        <div id="pageHelpSection">
            <blockquote>
                <a href="" id="pageHelp" style="display:inline;" onclick="openWindow('<html:rewrite action="/Organisation/ManageHelp"/>','onlinehelp',true,false,760,540); return false;">
                    <bean:message key="online.help.href"/>
                </a>
                <html:img page="/layout/help.gif" style="display:inline;"/>
            </blockquote>
        </div>

    </tiles:put>

</tiles:insert>

